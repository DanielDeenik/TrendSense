#!/bin/bash
# Simple script to run the SustainaTrend dashboard
# This script runs a pure Python implementation with no dependencies

echo "Starting SustainaTrend Dashboard..."
echo "This version uses only Python standard library"
echo "---------------------------------------------"

# Make the script executable
chmod +x simple_app.py

# Run the app
./simple_app.py